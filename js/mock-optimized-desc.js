/** @file 描述优化预览用 mock 文本（后端就绪前） */

export const MOCK_OPTIMIZED_DESCRIPTION = `# 称号定义（Title）

- **唯一 ID**：字符串，全局唯一标识。
- **类型（Type）**：枚举 \`PREFIX\` 或 \`SUFFIX\`，决定该称号占用前缀槽或后缀槽。
- **显示文本（Display Texts）**：称号可分别在三个 UI 场景独立配置显示内容，每个场景均支持静态文本或动态轮播文本；未单独配置的场景回退使用默认文本。
    - **默认文本（default）**：称号的基础显示文本，必须提供。可定义为静态文本，或一个文本数组用做动态轮播（动态文本仅留出空间，暂时不实际制作）。
    - **聊天文本（chat）**：在聊天消息中渲染的称号文本，未设置时使用默认文本。
    - **TAB 文本（tab）**：在玩家列表（Tab 列表）中渲染的称号文本，未设置时使用默认文本。
    - **头顶文本（nametag）**：在实体头顶名称标签中渲染的称号文本，未设置时使用默认文本。
- **静态文本**：定义为单个 Text 组件（JSON 文本，支持样式与事件），解析后始终保持不变。
- **动态文本**：暂不实现、但留出空间，定义为一个 \`Text[]\` 数组。
- **描述（description）**：可选的 Text 组件，用于命令界面、选择菜单或 API 中展示称号说明。
- **默认有效期（durationDays）**：整数，单位天，支持小数。\`-1\` 表示永久有效。过期后该称号对玩家自动失效（移除可用状态）。

---

# 指令模块

\`\`\`text
/playertitles give <玩家> <称号ID> [有效期]
\`\`\`

给予玩家某个称号的使用权。有效期参数支持小数天数（如 \`1.5\`），\`-1\` 表示永久。为空时使用称号默认有效期。

\`\`\`text
/playertitles remove <玩家> <称号ID>
\`\`\`

取消玩家某个称号的使用权。

\`\`\`text
/playertitles set <玩家> <称号ID>
\`\`\`

强制玩家装备某个称号，不检查玩家是否拥有该称号或是否过期。

\`\`\`text
/playertitles tempset <玩家> <称号ID> [有效期]
\`\`\`

临时覆盖玩家的装备称号，到期后自动恢复原有装备。有效期支持小数天数，\`-1\` 表示永久，为空时使用称号默认有效期。

\`\`\`text
/playertitles recover <玩家>
\`\`\`

卸下玩家的临时覆盖称号，恢复原有装备。

\`\`\`text
/playertitles list <玩家>
\`\`\`

列出该玩家已拥有的称号。

\`\`\`text
/managetitles create <称号ID> <类型> <默认文本> <描述> [默认文本颜色]
\`\`\`

简单地创建一个新称号，不提供更多参数（编辑工作主要在配置文件中进行）。

\`\`\`text
/managetitles reload
\`\`\`

重载全部称号配置文件。

\`\`\`text
/managetitles list
\`\`\`

列出所有已定义称号。

\`\`\`text
/mytitles
\`\`\`

打开箱子界面，展示玩家可用的前缀与后缀称号，允许选择佩戴或摘除。摘除按钮在配置 \`preventTitleRemoval\` 为 \`true\` 时变为不可用状态，并给出“禁止卸下称号”的提示。

---

# 配置文件模块

配置文件路径固定为 \`config/exampleplayertitles/config.json\`，在模组初始化时读取。

1. **chatPrefix**
   - 类型：布尔值
   - 默认值：\`true\`
   - 说明：控制是否在聊天消息中自动为玩家名称添加前缀。

2. **chatSuffix**
   - 类型：布尔值
   - 默认值：\`true\`
   - 说明：控制是否在聊天消息中自动为玩家名称添加后缀。

3. **tabListPrefix**
   - 类型：布尔值
   - 默认值：\`true\`
   - 说明：控制是否在 TAB 玩家列表中自动为玩家名称添加前缀。

4. **tabListSuffix**
   - 类型：布尔值
   - 默认值：\`true\`
   - 说明：控制是否在 TAB 玩家列表中自动为玩家名称添加后缀。

5. **nameTagPrefix**
   - 类型：布尔值
   - 默认值：\`false\`
   - 说明：控制是否在玩家头顶名称标签中自动添加前缀。因为头顶显示需要 Mixin 注入，为了在不提供 Mixin 时保持兼容，该项默认关闭。

6. **nameTagSuffix**
   - 类型：布尔值
   - 默认值：\`false\`
   - 说明：控制是否在玩家头顶名称标签中自动添加后缀。同样默认关闭，需配合 Mixin 使用。

7. **defaultPrefix**
   - 类型：JSON 文本组件（对象）
   - 默认值：\`{"text":""}\`
   - 说明：默认前缀，当玩家没有可用前缀或新玩家根据配置自动装备时使用。

8. **defaultSuffix**
   - 类型：JSON 文本组件（对象）
   - 默认值：\`{"text":""}\`
   - 说明：默认后缀，使用情景同上。

9. **preventTitleRemoval**
   - 类型：布尔值
   - 默认值：\`true\`
   - 说明：禁止玩家卸下称号。开启后，玩家无法通过命令或 GUI 将自己的称号设为空；GUI 中的摘除按钮变为无效并显示提示；当装备的称号因过期等原因变得不可用时，会自动替换为默认称号（\`defaultPrefix\`/\`defaultSuffix\`）而非留空。若关闭，不可用时称号位将被清空。

10. **equipDefaultOnFirstJoin**
    - 类型：布尔值
    - 默认值：\`true\`
    - 说明：新玩家首次加入服务器时，是否自动为其装备默认前缀与默认后缀。

11. **titleDataPath**
    - 类型：字符串
    - 默认值：\`"config/exampleplayertitles/titles.json"\`
    - 说明：内置配置文件来源所读取的称号定义文件路径。

12. **playerDataPath**
    - 类型：字符串
    - 默认值：\`"config/exampleplayertitles/playerdata/"\`
    - 说明：玩家个人称号数据的存储目录路径，每个玩家以 \`UUID.json\` 文件保存。

---

# 显示与装备逻辑补充

- **临时称号优先**：如果玩家存在有效的临时覆盖称号（通过 \`tempset\` 设置），则在该临时覆盖生效期间，所有显示场景（聊天、TAB、头顶）均使用临时称号，忽略玩家原本装备的前缀或后缀。

- **装备可用性检查**：当玩家主动装备称号或系统恢复装备时，会检查该称号对玩家是否可用（已拥有且未过期）。若不可用，则根据 \`preventTitleRemoval\` 配置处理：
    - 若 \`preventTitleRemoval\` 为 \`true\`，自动装备对应槽位的默认称号（\`defaultPrefix\`/\`defaultSuffix\`）。
    - 若为 \`false\`，将对应槽位清空（不显示任何称号）。

- **临时覆盖到期后恢复原有装备**，同样触发上述可用性检查。

- **强制装备指令（\`set\`）**忽略可用性检查，直接装备，且会覆盖当前的临时称号。

- **删除称号的功能已移除**，不再支持通过指令删除已定义的称号。`;
